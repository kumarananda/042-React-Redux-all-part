import { BLUE, GRAY, GREEN, RED, YELLOW } from "./actionTypes"


// make RED
export const makeRed = (payload) => {
    return {
        type : RED
    }
}

// make YELLOW
export const makeYellow = (payload) => {
    return {
        type : YELLOW
    }
}

// make GRAY
export const makeGray = (payload) => {
    return {
        type : GRAY
    }
}

// make GREEN
export const makeGreen = (payload) => {
    return {
        type : GREEN
    }
}

// make BLUE
export const makeBlue = (payload) => {
    return {
        type : BLUE
    }
}